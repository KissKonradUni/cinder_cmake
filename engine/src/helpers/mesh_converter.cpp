#include "helpers/mesh_converter.hpp"

namespace codex {

void LoadedMesh::convertUsingAssimp(const std::filesystem::path& filePath, const std::filesystem::path& outputPath) {

}

std::unique_ptr<LoadedMesh> LoadedMesh::loadFromFile(std::filesystem::path filePath) {
    
}

};